[pytest]
DJANGO_SETTINGS_MODULE = melee_game.settings
python_files = test_*.py
testpaths = engine/tests board/tests
# The rules engine is pure Python; most tests need no database.
addopts = -p no:cacheprovider
